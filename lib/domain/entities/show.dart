class Show {
  final String id;
  final String title;
  final String cover;
  final bool hasNewEpisode;
  final bool hasNft;

  const Show(this.id, this.title, this.cover, this.hasNewEpisode, this.hasNft);
}
